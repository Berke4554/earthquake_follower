import React, { useRef, useEffect, useState } from "react";
import {
  GoogleMap,
  Marker,
  MarkerF,
  Polyline,
  PolylineF,
  useJsApiLoader,
} from "@react-google-maps/api";

const MapWithMarkersAndLine = (props) => {
  return <></>;
};

export default MapWithMarkersAndLine;
